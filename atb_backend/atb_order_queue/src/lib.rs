#![cfg_attr(debug_assertions, allow(dead_code, unused_imports))]
// todo: drop orders if quantity is 0

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
